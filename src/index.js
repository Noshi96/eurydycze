"use strict";

import "./styles.css";

document.addEventListener("DOMContentLoaded", () => {
  const container = document.createElement("div");
  // container.classList.add("new-class");
  // container.innerHTML = "siema";
  // document.querySelector(".main-class").append(container);
  // document.querySelector(".header-class").append(container);
});
